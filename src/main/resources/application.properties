# 서버 포트번호
server.port=8090

# viewresolver 세팅
spring.mvc.view.prefix=/WEB-INF/views/
spring.mvc.view.suffix=.jsp

# spring-jdbc : datasource 설정
# (아래 설정 없으면 가동시 에러 발생)
# JDBC 드라이버 클래스 정보
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# DB 서버 정보
spring.datasource.url=jdbc:mysql://localhost:3306/mydb720?useSSL=false&serverTimeZone=Asia/Seoul&allowPublicKeyRetrieval=true
# DB 사용자 계정 아이디
spring.datasource.username=myuser720
# DB 사용자 계정 비밀번호
spring.datasource.password=1234

# MyBatis 매퍼파일 위치 설정
mybatis.mapper-locations=classpath:com/spring/domain/mapper/*.xml